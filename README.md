# codex-demo
完全使用 codex 写代码的测试仓库

## 结果展示页面
新增了一个本地 Web 页面，用于：
- 展示 `results/` 下的 JSON 文件列表并查看内容。
- 展示 `pdf_path/` 下的 PDF 文件列表并在线预览。
- 上传 JSON/PDF 文件到对应目录。

### 启动方式
```bash
python web_app.py
```
打开：`http://127.0.0.1:8000`
